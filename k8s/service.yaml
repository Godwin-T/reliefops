apiVersion: v1
kind: Service
metadata:
  name: reliefops-tracker
  labels:
    app: reliefops-tracker
spec:
  type: ClusterIP
  selector:
    app: reliefops-tracker
  ports:
    - name: http
      port: 80
      targetPort: 3000
